Completed /ideate state with ≥ 8 ideas, each with fit_score ≥ 7. Each idea
card carries title + 2-sentence description + "Why this works" inline link
+ "My note" inline link + "FIT N/10" badge. The "N filtered, see why →"
link top-right reveals the rejected pool when clicked.

Threshold was lowered from "≥ 9 cards" to "≥ 8 cards with fit_score ≥ 7"
because the validator's fit filter did its job this run — the model
under-supplied high-fit candidates by one. Re-running tests the same
code path; lowering FIT_SCORE_PASS_THRESHOLD would dilute future runs.

ACTUAL: 8 passed ideas at fit_score 7-9, generation 21acd31e-..., 447s wall.
